---
import Layout from "@layouts/Layout.astro";
import ItemViewer from "@components/inventory/ItemViewer.vue";

const { id } = Astro.params;
---

<Layout title="Item Details | Resale Command">
    <ItemViewer itemId={id} client:only="vue" />
</Layout>
